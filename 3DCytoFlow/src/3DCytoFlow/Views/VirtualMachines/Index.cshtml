@model IEnumerable<_3DCytoFlow.ViewModels.VirtualMachine.DetailsViewModel>

@{
    ViewBag.Title = "Connected Virtual Machines";
}

<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">@ViewBag.Title</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>

    <p>
        @Html.ActionLink("Add New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PointNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ETC)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
            {
            <tr>
                <th>
                    @Html.DisplayFor(modelItem => item.name)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.JobNumber)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.PointNumber)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.ETC)
                </th>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.id })
                </td>
            </tr>
        }

    </table>

</div>
